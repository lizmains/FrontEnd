<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiApp2.NextPage"
             Title="NextPage">
    <VerticalStackLayout
        VerticalOptions="Center">
        <!--<Button 
                x:Name="PrevBtn"
                Text="Previous Page"
                SemanticProperties.Hint="Navigate to first page when you click"
                Clicked="OnPrevBtnClicked"
                HorizontalOptions="Center" 
            VerticalOptions="Center"/>-->
        <Button 
            x:Name="SimBtn"
            Text="View Simulator"
            SemanticProperties.Hint="Navigate to sim page when you click"
            Clicked="OnSimBtnClicked"
            HorizontalOptions="Center" />
        <Button 
            x:Name="VidBtn"
            Text="Analyze Throw"
            SemanticProperties.Hint="Navigate to throw video analyzer page"
            Clicked="OnVidBtnClicked"
            HorizontalOptions="Center" />
        <Button 
            x:Name="HomeBtn"
            Text="Home"
            SemanticProperties.Hint="Navigate to main page when you click"
            Clicked="OnHomeBtnClicked"
            HorizontalOptions="Center" />
    </VerticalStackLayout>
</ContentPage>